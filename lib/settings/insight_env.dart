// ============================================================
// INSIGHT ENV — Microsoft Clarity project id
// ============================================================
// [FINGERPRINT] — one Clarity project per application, never reuse
// across other gray-flow builds. Replace the placeholder before
// shipping; SDK will no-op if the id is empty.
// ============================================================

const String kClarityProjectId = 'PASTE_PROJECT_ID_HERE';
